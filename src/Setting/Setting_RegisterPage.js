import React, { Component } from "react";
import { RegisterPage, Header } from "../Page";

export default class SettingRegisterPage extends Component {
  render() {
    return (
      <div>
        <Header />
        <RegisterPage />
      </div>
    );
  }
}
