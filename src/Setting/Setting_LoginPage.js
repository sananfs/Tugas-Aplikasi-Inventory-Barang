import React, { Component } from "react";
import { LoginPage, Header } from "../Page";

export default class SettingLoginPage extends Component {
  render() {
    return (
      <div>
        <Header />
        <LoginPage />
      </div>
    );
  }
}
